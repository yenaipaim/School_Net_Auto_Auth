<Page
    x:Class="SchoolNetAutoAuth.App.Views.NetworkSettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch">
    <ScrollViewer VerticalScrollMode="Enabled" VerticalScrollBarVisibility="Auto"
                  HorizontalScrollMode="Disabled" HorizontalScrollBarVisibility="Disabled">
        <StackPanel MaxWidth="680" Padding="32" HorizontalAlignment="Left" Spacing="18">
            <StackPanel Spacing="6">
                <TextBlock Text="网络设置" Style="{ThemeResource TitleTextBlockStyle}" />
                <TextBlock Text="配置目标网络、认证入口和检测频率" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
            </StackPanel>
            <TextBox Header="目标 Wi-Fi" Text="{Binding TargetSsid, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
            <TextBox Header="认证地址" Text="{Binding PortalUri, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
            <TextBox Header="联网检测地址" Text="{Binding ProbeUri, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
            <NumberBox Header="网络检测间隔（秒）" Value="{Binding NetworkCheckIntervalSeconds, Mode=TwoWay}"
                       Minimum="3" Maximum="3600" SpinButtonPlacementMode="Inline" />
            <ToggleSwitch Header="自动认证" IsOn="{Binding AutomaticAuthenticationEnabled, Mode=TwoWay}" />
            <Button Command="{Binding SaveCommand}" Style="{StaticResource AccentButtonStyle}" HorizontalAlignment="Left" Padding="18,9">
                <StackPanel Orientation="Horizontal" Spacing="8"><SymbolIcon Symbol="Save" /><TextBlock Text="保存设置" /></StackPanel>
            </Button>
        </StackPanel>
    </ScrollViewer>
</Page>
