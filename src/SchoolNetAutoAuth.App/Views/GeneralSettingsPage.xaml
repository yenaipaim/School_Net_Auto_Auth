<Page
    x:Class="SchoolNetAutoAuth.App.Views.GeneralSettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch">
    <ScrollViewer VerticalScrollMode="Enabled" VerticalScrollBarVisibility="Auto"
                  HorizontalScrollMode="Disabled" HorizontalScrollBarVisibility="Disabled">
        <StackPanel MaxWidth="820" Padding="32" HorizontalAlignment="Left" Spacing="24">
            <StackPanel Spacing="6">
                <TextBlock Text="通用设置" Style="{ThemeResource TitleTextBlockStyle}" />
                <TextBlock Text="版本、作者、外观与交流群" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
            </StackPanel>

            <StackPanel Spacing="12">
                <TextBlock Text="版本更新" Style="{ThemeResource SubtitleTextBlockStyle}" />
                <Grid ColumnSpacing="16">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <StackPanel Spacing="3" VerticalAlignment="Center">
                        <TextBlock Text="当前版本" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                        <TextBlock Text="{Binding CurrentVersion}" FontSize="18" FontWeight="SemiBold" />
                    </StackPanel>
                    <Button Grid.Column="1" Command="{Binding CheckForUpdatesCommand}"
                            IsEnabled="{Binding CanCheckUpdates}" Style="{StaticResource AppButtonStyle}">
                        <StackPanel Orientation="Horizontal" Spacing="7">
                            <SymbolIcon Symbol="Refresh" />
                            <TextBlock Text="检查更新" />
                        </StackPanel>
                    </Button>
                </Grid>
            </StackPanel>

            <Rectangle Height="1" Fill="{ThemeResource DividerStrokeColorDefaultBrush}" />

            <StackPanel Spacing="10">
                <TextBlock Text="作者" Style="{ThemeResource SubtitleTextBlockStyle}" />
                <TextBlock Text="{Binding Author}" FontSize="18" FontWeight="SemiBold" />
                <HyperlinkButton Content="{Binding RepositoryUri}" NavigateUri="{Binding RepositoryUri}" Padding="0" />
            </StackPanel>

            <Rectangle Height="1" Fill="{ThemeResource DividerStrokeColorDefaultBrush}" />

            <StackPanel Spacing="12">
                <TextBlock Text="背景图像" Style="{ThemeResource SubtitleTextBlockStyle}" />
                <TextBlock Text="{Binding BackgroundImageText}" TextWrapping="Wrap"
                           Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                <Grid ColumnSpacing="12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="48" />
                    </Grid.ColumnDefinitions>
                    <Slider Header="透明度" Minimum="0" Maximum="100" StepFrequency="1"
                            Value="{Binding BackgroundImageOpacityPercent, Mode=TwoWay}" />
                    <TextBlock Grid.Column="1" Text="{Binding BackgroundImageOpacityText}"
                               VerticalAlignment="Bottom" HorizontalAlignment="Right" />
                </Grid>
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <Button Command="{Binding ChooseBackgroundImageCommand}" Style="{StaticResource AppButtonStyle}">
                        <StackPanel Orientation="Horizontal" Spacing="7">
                            <SymbolIcon Symbol="Folder" />
                            <TextBlock Text="选择图像" />
                        </StackPanel>
                    </Button>
                    <Button Command="{Binding ClearBackgroundImageCommand}" Style="{StaticResource AppButtonStyle}">
                        <StackPanel Orientation="Horizontal" Spacing="7">
                            <SymbolIcon Symbol="Delete" />
                            <TextBlock Text="清除" />
                        </StackPanel>
                    </Button>
                </StackPanel>
            </StackPanel>

            <Rectangle Height="1" Fill="{ThemeResource DividerStrokeColorDefaultBrush}" />

            <StackPanel Spacing="10">
                <TextBlock Text="交流群" Style="{ThemeResource SubtitleTextBlockStyle}" />
                <TextBlock Text="{Binding GroupNumber}" FontSize="20" FontWeight="SemiBold" />
                <Image Source="ms-appx:///Assets/QQ.png" Width="240" Stretch="Uniform"
                       HorizontalAlignment="Left" />
            </StackPanel>
        </StackPanel>
    </ScrollViewer>
</Page>
